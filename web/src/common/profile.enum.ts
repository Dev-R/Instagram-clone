/**
 * Enums for profile tabs.
 */
export enum ProfileTab {
    Posts = 'profile-posts', // Represents the posts tab
    Peeds = 'profile-peed', // Represents the peeds tab
    Saved = 'profile-saved', // Represents the saved tab
    Tagged = 'profile-tagged', // Represents the tagged tab
}
/**
 * Enums for profile modals.
 */
export enum ProfileTriggeredModal {
    Follow = 'follow-modal', // Represents the follow modal
    Profile = 'profile-modal', // Represents the profile modal
    Setting = 'setting-modal', // Represents the setting modal
    Comment = 'comment-modal', // Represents the comment modal
}